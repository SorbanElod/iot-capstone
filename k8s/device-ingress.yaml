apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: device-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: device-app-service
            port:
              number: 80